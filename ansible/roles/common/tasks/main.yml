---
- name: Update apt cache
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install essential packages
  apt:
    name:
      - python3-pip
      - python3-venv
      - curl
      - git
      - vim
      - htop
      - tree
      - net-tools
      - wget
    state: present