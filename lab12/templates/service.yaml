apiVersion: v1
kind: Service
metadata:
  name: lab12-app
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
  selector:
    app: lab12-app
